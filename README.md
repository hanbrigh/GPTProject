# Adapted from Andrej Karpathy’s “Let’s Build GPT” tutorial
Built training pipeline that optimized runtime efficiency, allowing users to generate text based off input without retraining the model each time.
